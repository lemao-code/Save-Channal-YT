export default class UserSchema {
    static schema = {
        name: "fav",
        properties: {
            name: "string",
            idChannel: "string",
            img: "string"
        }
    }
}